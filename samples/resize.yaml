---

schema: "yardstick:task:0.1"
{% set image = image or "cirros-0.3.3" %}
scenarios:
-
  type: Resize
  host: node1.resize

  runner:
    type: Iteration
    iteration: 1

  vm2_origin_flavor: yardstick-flavor
  vm2_new_flavor: m1.small
  vm2_image: {{ image }}

  vm1_new_flavor: m1.small

context:
  name: resize
  image: {{image}}
  flavor: yardstick-flavor
  user: cirros

  servers:
    node1:
      floating_ip: true

  networks:
    test:
      cidr: '10.0.1.0/24'
